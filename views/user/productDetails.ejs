<!DOCTYPE html>
<html lang="zxx">

<head>
    <meta charset="UTF-8">
    <meta name="description" content="Male_Fashion Template">
    <meta name="keywords" content="Male_Fashion, unica, creative, html">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta http-equiv="X-UA-Compatible" content="ie=edge">
    <title>Zay fashion</title>

    <!-- Google Font -->
    <link href="https://fonts.googleapis.com/css2?family=Nunito+Sans:wght@300;400;600;700;800;900&display=swap"
        rel="stylesheet">

    <!-- Css Styles -->
    <link rel="stylesheet" href="/css/bootstrap.min.css" type="text/css">
    <link rel="stylesheet" href="/css/font-awesome.min.css" type="text/css">
    <link rel="stylesheet" href="/css/elegant-icons.css" type="text/css">
    <link rel="stylesheet" href="/css/magnific-popup.css" type="text/css">
    <link rel="stylesheet" href="/css/nice-select.css" type="text/css">
    <link rel="stylesheet" href="/css/owl.carousel.min.css" type="text/css">
    <link rel="stylesheet" href="/css/slicknav.min.css" type="text/css">
    <link rel="stylesheet" href="/css/style.css" type="text/css">
    <link rel="stylesheet" href="assets2/css/bootstrap.min.css">
    <link rel="stylesheet" href="assets2/css/templatemo.css">
    <link rel="stylesheet" href="assets2/css/custom.css">
     <!-- Load fonts style after rendering the layout styles -->
     <link rel="stylesheet" href="https://fonts.googleapis.com/css2?family=Roboto:wght@100;200;300;400;500;700;900&display=swap">
     <link rel="stylesheet" href="assets2/css/fontawesome.min.css">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.1/css/all.min.css"
        integrity="sha512-DTOQO9RWCH3ppGqcWaEA1BIZOC6xxalwEsw9c2QQeAIftl+Vegovlnee1c9QX4TctnWMn13TZye+giMm8e2LwA=="
        crossorigin="anonymous" referrerpolicy="no-referrer" />

        <style>
            #snackbar {
              visibility: hidden;
              min-width: 400px;
              margin-left: -125px;
              background-color: #000000;
              color: #fff;
              text-align: center;
              border-radius: 2px;
              padding: 40px;
              position: fixed;
              z-index: 1;
              left: 50%;
              bottom: 30px;
              font-size: 17px;
            }
            
            #snackbar.show {
              visibility: visible;
              -webkit-animation: fadein 0.5s, fadeout 0.5s 2.5s;
              animation: fadein 0.5s, fadeout 0.5s 2.5s;
            }
            .show {
                visibility: visible;
              -webkit-animation: fadein 0.5s, fadeout 0.5s 2.5s;
              animation: fadein 0.5s, fadeout 0.5s 2.5s;
            }
            
            @-webkit-keyframes fadein {
              from {bottom: 0; opacity: 0;} 
              to {bottom: 30px; opacity: 1;}
            }
            
            @keyframes fadein {
              from {bottom: 0; opacity: 0;}
              to {bottom: 30px; opacity: 1;}
            }
            
            @-webkit-keyframes fadeout {
              from {bottom: 30px; opacity: 1;} 
              to {bottom: 0; opacity: 0;}
            }
            
            @keyframes fadeout {
              from {bottom: 30px; opacity: 1;}
              to {bottom: 0; opacity: 0;}
            }

            #wishlist {
              visibility: hidden;
              min-width: 400px;
              margin-left: -125px;
              background-color: #000000;
              color: #fff;
              text-align: center;
              border-radius: 2px;
              padding: 40px;
              position: fixed;
              z-index: 1;
              left: 50%;
              bottom: 30px;
              font-size: 17px;
            }
            
            #wishlist.show {
              visibility: visible;
              -webkit-animation: fadein 0.5s, fadeout 0.5s 2.5s;
              animation: fadein 0.5s, fadeout 0.5s 2.5s;
            }
            .show {
                visibility: visible;
              -webkit-animation: fadein 0.5s, fadeout 0.5s 2.5s;
              animation: fadein 0.5s, fadeout 0.5s 2.5s;
            }
            
            @-webkit-keyframes fadein {
              from {bottom: 0; opacity: 0;} 
              to {bottom: 30px; opacity: 1;}
            }
            
            @keyframes fadein {
              from {bottom: 0; opacity: 0;}
              to {bottom: 30px; opacity: 1;}
            }
            
            @-webkit-keyframes fadeout {
              from {bottom: 30px; opacity: 1;} 
              to {bottom: 0; opacity: 0;}
            }
            
            @keyframes fadeout {
              from {bottom: 30px; opacity: 1;}
              to {bottom: 0; opacity: 0;}
            }
            </style>
</head>

<body >
    <!-- Page Preloder -->
    <div id="preloder">
        <div class="loader"></div>
    </div>

    <!-- Header Section Begin -->
    <%- include('../partials/nav.ejs') -%>
        <!-- Header Section End -->

        <!-- Shop Details Section Begin -->
        <section class="shop-details">
            <div class="product__details__pic">
                <div class="container-fluid">
                    <div class="row">
                        <div class="col-lg-12">
                            <div class="product__details__breadcrumb">
                                <a href="/">Home</a>
                                <a href="/shop">Shop</a>
                                <span>Product Details</span>
                            </div>
                        </div>
                    </div>
                    <div class="row">
                        <div class="col-lg-1 col-md-3 ml-lg-5">
                            <ul class="nav nav-tabs" role="tablist">
                                <% for (let i=0; i < product.variant[index].images.length; i++) { %>
                                    <li class="nav-item">
                                        <a class="nav-link <%= i === 0 ? 'active' : '' %>" data-toggle="tab"
                                            href="#tabs-<%= i + 1 %>" role="tab">
                                            <div class="div focus"
                                                data-setbg="/img/shop-details/thumb-<%= i + 1 %>.png">
                                                <img class="hower"
                                                    src="/img/productImage/sharp/<%= product.variant[index].images[i] %>"
                                                    alt="">
                                            </div>
                                        </a>
                                    </li>
                                    <% } %>
                            </ul>
                        </div>

                        <div class="col-lg-4 col-md-9">
                            <div class="tab-content">
                                <% for (let i=0; i < product.variant[index].images.length; i++) { %>
                                    <div class="tab-pane <%= i === 0 ? 'active' : '' %>" id="tabs-<%= i + 1 %>"
                                        role="tabpanel">
                                        <div class="product__details__pic__item image-zoom-container"
                                            style="overflow: hidden;">
                                            <!-- Use unique IDs for each image and class 'zoom-trigger' for event binding -->
                                            <img id="zoom-image-<%= i + 1 %>" class="zoom-trigger"
                                                src="/img/productImage/<%= product.variant[index].images[i] %>" alt="">
                                        </div>
                                    </div>
                                    <% } %>
                            </div>
                        </div>




                        <div class="col-lg-5">
                            <div class=" ">
                                <h4 data-index="0" id="productName" class="p-2">
                                    <%= product.name %>
                                </h4>
                                <div class="rating">
                                    <%for(let i = 1; i <= 5 ; i++) {%>
                                       <%if(totalRating < i) {%>
                                        <i class="fa fa-star fa-regular"></i>
                                       <%} else {%>
                                        <i class="fa fa-star"></i>
                                       <%}%>
                                        
                                    <%}%>
                                </div>
                                <%const discount=(product.variant[index].price - product.variant[index].offerPrice) /
                                    product.variant[index].price%>
                                    <%const percentageDiscount=discount * 100%>
                                        <%const off=Math.floor(percentageDiscount)%>
                                            <h3 class="p-2">₹<del><%=
                                                product.variant[index].price%></del><span> ₹<%= product.variant[index].offerPrice%></span> <span class=""
                                                        style="color: rgb(51, 181, 51); text-decoration: none; font-size: 16px;">
                                                        <%= off %>% off
                                                    </span></h3>
                                            <p class="p-2">
                                                <%= product.description%>
                                            </p>
                                            <div class="product__details__option">
                                                <div class="product__details__option__size" style="position: relative;">
                                                    <span>Size:</span>
                                                    <%product.variant[index].size.map((el, i)=> {%>
                                                        <label class="" for="<%= el%>">
                                                            <%= el%>
                                                                <input type="radio" id="<%= el %>" value="<%= el%>"
                                                                    name="size">
                                                        </label>
                                                        <%})%>

                                                            <div id="size" style="position: absolute; left: 50px;">

                                                            </div>

                                                </div>
                                                <div class="product__details__option__color">
                                                    <span>Color:</span>

                                                    <%product.variant.map((el, i)=> {%>
                                                        <span style="width: 50px; height: 50px;"
                                                            onclick="selectVariant('<%= product._id%>', '<%= i %>')"
                                                            class="c-1 variant" for="sp-1">

                                                            <img src="/img/productImage/sharp/<%= el.images[0] %>"
                                                                alt="">

                                                        </span>
                                                        <%})%>

                                                </div>
                                            </div>
                                            <div class="product__details__cart__option">
                                                <% if (product.variant[index].stock === 0) { %>
                                                    <button type="button" class="primary-btn" disabled>Out of Stock</button>
                                                <% } else { %>
                                                    <div class="quantity buttons_added pro-qty">
                                                        <input type="button" value="-" class="minus dec qtybtn">
                                                        <input type="number" id="quantity" min="1" max="<%= product.variant[index].stock %>" data-stock="<%= product.variant[index].stock %>" name="quantity" value="1" title="Qty" class="input-text qty text" pattern="" inputmode="">
                                                        <input type="button" value="+" class="plus inc qtybtn">
                                                    </div>
                                                    <button type="button" onclick="addToCart('<%= product._id %>');" class="primary-btn">Add to Cart</button>                                                    
                                                    <% if (product.variant[index].stock > 0 && product.variant[index].stock < 5) { %>
                                                        <div class="alert alert-warning mt-2" role="alert">
                                                            Hurry, only <%= product.variant[index].stock %> left in stock!
                                                        </div>
                                                    <% } %>
                                                <% } %>
                                            </div>
                                            
                                            <div class="product__details__btns__option">
                                                <button type="button" class="btn" onclick="addTOWishlist('<%= product._id%>')"><i class="fa fa-heart"></i> add to wishlist</button>
                                               
                                            </div>

                            </div>
                        </div>
                    </div>

                  
                    <div id="snackbar">
                        <span style="border-radius: 50%; background-color: #24c20f; padding: 9px 10px;"><i class="fa-solid fa-check fa-lg" style="color: white;">
                        </i></span>
                        <span style="margin-left: 10px;">Item successfully added to your cart!</span>
                       
                    </div>

                    <div id="wishlist">
                        <span style="border-radius: 50%; background-color: #24c20f; padding: 9px 10px;"><i class="fa-solid fa-check fa-lg" style="color: white;">
                        </i></span>
                        <span style="margin-left: 10px;">Item successfully added to your wishlist!</span>
                       
                    </div>


                </div>
            </div>
            </div>
            <div class="product__details__content">
                <div class="container">
                    <div class="row d-flex justify-content-center">

                    </div>
                    <div class="row">
                        <div class="col-lg-12">
                            <div class="product__details__tab">
                                <ul class="nav nav-tabs" role="tablist">
                                    
                                    <li class="nav-item">
                                        <a class="nav-link" data-toggle="tab" href="#tabs-6" role="tab">Customer
                                            reviews(21)</a>
                                    </li>
            
                                </ul>
                                <div class="tab-content">
                                    
                                    <div class="tab-pane" id="tabs-6" role="tabpanel">
                                        <div class="product__details__tab__content">
                                           
                                            
                                        </div>
                                    </div>
                                </div>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </section>
        <!-- Shop Details Section End -->

        <!-- Related Section Begin -->
        <section class="bg-light">
            <div class="container py-5">
                <div class="row text-center py-3">
                    <div class="col-lg-6 m-auto">
                        <h1 class="h1">Related Products</h1>
                        <p>
                            Reprehenderit in voluptate velit esse cillum dolore eu fugiat nulla pariatur.
                            Excepteur sint occaecat cupidatat non proident.
                        </p>
                    </div>
                </div>
                <div class="row">
                    <div class="col-12 col-md-4 mb-4">
                        <div class="card h-100">
                            <%const length = related.length%>
                            <a href="/productDetails?id=<%= related[length - 2]._id%>&index=0">
                                <img src="/img/productImage/sharp/<%= related[length - 2].variant[0].images[0]%>" class="card-img-top" alt="...">
                            </a>
                            <div class="card-body">
                                <ul class="list-unstyled d-flex justify-content-between">
                                    <li>
                                        <i class="text-warning fa fa-star"></i>
                                        <i class="text-warning fa fa-star"></i>
                                        <i class="text-warning fa fa-star"></i>
                                        <i class="text-muted fa fa-star"></i>
                                        <i class="text-muted fa fa-star"></i>
                                    </li>
                                    <li class="text-muted text-right"><%=related[length - 2].brand%></li>
                                </ul>
                                <a href="/productDetails?id=<%= related[length - 2]._id%>&index=0" class="h2 text-decoration-none text-dark"><%=related[length - 2].name%></a>
                                <p class="card-text">
                                    <%=related[length - 2].description%>
                                </p>
                                <p class="text-muted">Reviews (24)</p>
                            </div>
                        </div>
                    </div>
                    <div class="col-12 col-md-4 mb-4">
                        <div class="card h-100">
                            <a href="/productDetails?id=<%= related[length - 1]._id%>&index=0">
                                <img src="/img/productImage/sharp/<%= related[length - 1].variant[0].images[0]%>"  class="card-img-top" alt="...">
                            </a>
                            <div class="card-body">
                                <ul class="list-unstyled d-flex justify-content-between">
                                    <li>
                                        <i class="text-warning fa fa-star"></i>
                                        <i class="text-warning fa fa-star"></i>
                                        <i class="text-warning fa fa-star"></i>
                                        <i class="text-muted fa fa-star"></i>
                                        <i class="text-muted fa fa-star"></i>
                                    </li>
                                    <li class="text-muted text-right"><%=related[length - 1].brand%></li>
                                </ul>
                                <a href="shop-single.html" class="h2 text-decoration-none text-dark"><%=related[length - 1].name%></a>
                                <p class="card-text">
                                    <%=related[length - 1].description%>
                                </p>
                                <p class="text-muted">Reviews (48)</p>
                            </div>
                        </div>
                    </div>
                    <div class="col-12 col-md-4 mb-4">
                        <div class="card h-100">
                            <a href="/productDetails?id=<%= related[length - 3]._id%>&index=0">
                                <img src="/img/productImage/sharp/<%= related[length - 3].variant[0].images[0]%>"class="card-img-top" alt="...">
                            </a>
                            <div class="card-body">
                                <ul class="list-unstyled d-flex justify-content-between">
                                    <li>
                                        <i class="text-warning fa fa-star"></i>
                                        <i class="text-warning fa fa-star"></i>
                                        <i class="text-warning fa fa-star"></i>
                                        <i class="text-warning fa fa-star"></i>
                                        <i class="text-warning fa fa-star"></i>
                                    </li>
                                    <li class="text-muted text-right"><%=related[length - 3].brand%></li>
                                </ul>
                                <a href="/productDetails?id=<%= related[length - 3]._id%>&index=0" class="h2 text-decoration-none text-dark"><%=related[length - 3].name%></a>
                                <p class="card-text">
                                    <%=related[length - 3].description%>
                                </p>
                                <p class="text-muted">Reviews (74)</p>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </section>
        <!-- Related Section End -->

        <!-- Footer Section Begin -->
        <%- include('../layout/userTemplateFooter.ejs') -%>
        <!-- Footer Section End -->

        <!-- Search Begin -->
        <div class="search-model">
            <div class="h-100 d-flex align-items-center justify-content-center">
                <div class="search-close-switch">+</div>
                <form class="search-model-form">
                    <input type="text" id="search-input" placeholder="Search here.....">
                </form>
            </div>
        </div>
        <!-- Search End -->

        <!-- Js Plugins -->
        <script src="/js/jquery-3.3.1.min.js"></script>
        <script src="/js/bootstrap.min.js"></script>
        <script src="/js/jquery.nice-select.min.js"></script>
        <script src="/js/jquery.nicescroll.min.js"></script>
        <script src="/js/jquery.magnific-popup.min.js"></script>
        <script src="/js/jquery.countdown.min.js"></script>
        <script src="/js/jquery.slicknav.js"></script>
        <script src="/js/mixitup.min.js"></script>
        <script src="/js/owl.carousel.min.js"></script>
        <script src="/js/main.js"></script>
        <script src="/js/user/cart.js"></script>
        <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"
         integrity="sha384-C6RzsynM9kWDrMNeT87bh95OGNyZPhcTNXj1NW7RuBCsyN/o0jlpcV8Qyq46cDfL" crossorigin="anonymous"></script>
         
</body>

</html>




<script>

    function selectVariant(pId, pIndex) {
        // const data = {
        //     id: pId,
        //     index: pIndex,

        // }
        $.ajax({
            type: "Get",
            url: `/productDetails?index=${pIndex}&id=${pId}`,



            success: ((response) => {
                if (response.product) {

                    document.getElementById('productName')
                        .setAttribute('data-index', response.index);

                    document.getElementById('quantity').setAttribute('data-stock', response.product.variant[response.index].stock)



                    // Update the navigation tabs
                    const navTabs = document.querySelectorAll('.nav-tabs .nav-link');
                    navTabs.forEach((tab, i) => {
                        tab.classList.toggle('active', i === 0); // Activate the first tab

                        const img = tab.querySelector('img');

                        console.log(response.index);
                        if (img) {
                            img.setAttribute('src', `/img/productImage/sharp/${response.product.variant[response.index].images[i]}`);
                        }

                    });

                    // Update the tab content
                    const tabContent = document.querySelectorAll('.tab-content .tab-pane');
                    tabContent.forEach((tab, i) => {
                        tab.classList.toggle('active', i === 0); // Show the content of the first tab
                        const img = tab.querySelector('img');
                        if (img) {
                            console.log('hello')
                            img.setAttribute('src', `/img/productImage/${response.product.variant[response.index].images[i]}`);
                        }

                    });




                }
            })
        })
    }
</script>

<script>


    const div = document.querySelectorAll('.focus');
    console.log(div)

    div.forEach(function (img, i) {
        div[0].style.border = '2px solid red';
        img.style.opacity = 0.5;

        img.addEventListener('click', function (event) {

            for (let i = 0; i < div.length; i++) {
                div[i].style.opacity = 0.5;
                div[i].style.border = '';


            }
            div[i].style.opacity = 1;
            this.style.border = '2px solid red'
            img.firstChild.style.transform = 'scale(.2)'


        });
        div[0].style.opacity = 1;

        // img.addEventListener('mouseover', () => {
        //     div[i].style.opacity = 1;


        // })
        // img.addEventListener('mouseleave', () => {
        //     div[i].style = '';
        // })

    });
    const span = document.querySelectorAll('.variant');
    console.log(div)

    span.forEach(function (img, i) {
        span[0].style.border = '2px solid red'
        img.addEventListener('click', function (event) {

            for (let i = 0; i < span.length; i++) {
                span[i].style.border = '';
            }

            this.style.border = '2px solid red'
            img.firstChild.style.transform = 'scale(.2)'

        });

    });





</script>


<script>
    // Use a class to select all images
    document.querySelectorAll('.zoom-trigger').forEach(function (img) {
        img.addEventListener('mouseover', function () {
            const myimage = this; // 'this' refers to the current image
            imageZoom(myimage);
        });
    });

    function imageZoom(img) {
        var container = img.parentElement;

        container.addEventListener('mousemove', function (e) {
            var rect = container.getBoundingClientRect();
            var x = e.clientX - rect.left; // X position within the container
            var y = e.clientY - rect.top; // Y position within the container

            var percentX = (x / container.offsetWidth) * 100;
            var percentY = (y / container.offsetHeight) * 100;

            img.style.transformOrigin = percentX + '% ' + percentY + '%';

            img.style.transform = 'scale(2)';
        });

        container.addEventListener('mouseleave', function () {
            img.style.transform = 'scale(1)';
        });
    }
</script>

<script>
    var quantityInput = document.getElementById('quantity');
    var decBtn = document.querySelector('.dec');
    var incBtn = document.querySelector('.inc');
    var addToCartBtn = document.querySelector('.primary-btn'); // Assuming this is your add to cart button
    const stock = quantityInput.getAttribute('data-stock');
    var maxStock = parseInt(stock);

    // Set the max attribute of the input field
    quantityInput.max = maxStock;

    // Event listener for decrement button
    decBtn.addEventListener('click', function () {
        var currentValue = parseInt(quantityInput.value) || 1;

        if (currentValue > 1) {
            quantityInput.value = currentValue - 1;
        }
    });

    // Event listener for increment button
    incBtn.addEventListener('click', function () {
        var currentValue = parseInt(quantityInput.value) || 0;

        if (currentValue < maxStock) {
            quantityInput.value = currentValue + 1;
        } else {
            showSnackbar('Cannot add more than ' + maxStock + ' items to cart.');
        }
    });

    // Event listener for adding to cart button click
    addToCartBtn.addEventListener('click', function () {
        var quantity = parseInt(quantityInput.value);

        // AJAX request to backend to add to cart
        var productId = '<%= product._id %>'; // Assuming you have this value available
        var addToCartUrl = '/add-cart'; // Replace with your actual endpoint

        // AJAX POST request
        $.ajax({
            type: 'POST',
            url: addToCartUrl,
            data: {
                productId: productId,
                quantity: quantity
            },
            success: function (response) {
                // Handle success response (if needed)
                console.log('Product added to cart successfully');
                // Show success message or redirect to cart page
                showSnackbar('Item successfully added to your cart!');
            },
            error: function (xhr, status, error) {
                // Handle error response from server
                var errorMessage = xhr.responseJSON.error || 'Error adding to cart.';
                console.error('Error adding to cart:', errorMessage);
                // Display error message to the user
                showSnackbar(errorMessage);
            }
        });
    });

    // Function to show snackbar messages
    function showSnackbar(message) {
        var snackbar = document.getElementById('snackbar');
        snackbar.textContent = message;
        snackbar.classList.add('show');
        setTimeout(function () {
            snackbar.classList.remove('show');
        }, 3000); // Hide after 3 seconds
    }
</script>






